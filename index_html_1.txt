<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Стежка</title>
  <link rel="icon" href="favicon.ico" type="image/x-icon">
  <style>
    /* Общие стили страницы */
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      width: 100%;
      font-family: "Segoe UI", sans-serif;
      background: url("stzhk_11_st.jpg") no-repeat center center fixed;
      background-size: cover;
      color: #5a3b2f;
      overflow: hidden;
    }

    /* Полупрозрачный слой для лучшей читаемости текста */
    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(255, 255, 255, 0.2);
      z-index: 0;
    }

    /* Контейнер для текста и меню */
    .content {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100%;
      text-align: center;
      animation: fadein 1.5s ease-in-out;
    }

    h1 {
      font-family: "Comic Sans MS", "Trebuchet MS", sans-serif;
      font-size: 5em;
      margin-bottom: 0.5em;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.15);
    }

    nav a {
      text-decoration: none;
      color: #5a3b2f;
      margin: 0 15px;
      font-size: 1.2em;
      font-weight: 500;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: #8b5e3c;
    }

    /* Анимация появления */
    @keyframes fadein {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Адаптивность */
    @media (max-width: 768px) {
      h1 {
        font-size: 3em;
      }
      nav a {
        display: block;
        margin: 10px 0;
        font-size: 1.3em;
      }
    }
  </style>
</head>

<body>
  <div class="content">
    <h1>Стежка</h1>
    <nav>
      <a href="#">Карта</a>
      <a href="#">Тур</a>
      <a href="#">Блог</a>
      <a href="#">Про нас</a>
    </nav>
  </div>
</body>
</html>
